# Quick Start

This page walks through a minimal example that uses `bunsen` alongside `burn`.

> **TODO:** flesh out with a worked example once the umbrella crate's public
> API stabilizes. The intent is a 20&ndash;40 line snippet that demonstrates a
> `bunsen` block, a tensor contract, and a `bunsen::support` test helper in
> the same file.

```rust,ignore
use burn::tensor::Tensor;
use bunsen::prelude::*;

// fn main() { /* ... */ }
```
