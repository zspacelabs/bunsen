# Writing a `Module`

A short tutorial on building a `burn::module::Module` that uses `bunsen::burner`
for lifecycle and `bunsen::contracts` for shape checking.

> **TODO:** walk through a small custom block end-to-end.
