# Testing with `bunsen::support`

`bunsen::support` exposes the helpers we use to test `bunsen` itself; they're
intentionally public so downstream crates can use them too.

> **TODO:** examples of `assert_close`, tensor fixture helpers, and
> backend-parameterized tests.
