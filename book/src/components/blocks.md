# `bunsen::blocks`

A library of reusable `burn::module::Module` components: simple inner layers,
recurrent utility blocks, and full model families.

> **TODO:** catalog the public blocks. Each entry should describe the math,
> the expected input/output [contracts](../concepts/contracts.md), and a
> short usage snippet.

API: <https://docs.rs/bunsen/latest/bunsen/blocks/>
