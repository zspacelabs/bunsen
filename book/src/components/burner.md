# `bunsen::burner`

`Module` lifecycle utilities that extend `burn`'s default behavior.

> **TODO:** document the lifecycle stages and the derive macros / traits that
> hook into them.

API: <https://docs.rs/bunsen/latest/bunsen/burner/>
