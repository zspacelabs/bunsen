# `bunsen::support`

Shared support code: assertion helpers, fixture builders, and testing utilities
that downstream crates can also use in their own test suites.

> **TODO:** list the test helpers and show how to depend on `bunsen::support`
> as a dev-dependency.

API: <https://docs.rs/bunsen/latest/bunsen/support/>
