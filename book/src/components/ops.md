# `bunsen::ops`

Additional `burn::tensor::Tensor` operations not currently in `burn` core.

> **TODO:** list operators, group them by purpose (reduction, reshape,
> sampling, ...), and note backend coverage.

API: <https://docs.rs/bunsen/latest/bunsen/ops/>
