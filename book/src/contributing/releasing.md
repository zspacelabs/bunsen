# Releasing

`bunsen`'s major/minor version tracks the `burn` release it builds against.

> **TODO:** document the release checklist (bump version, update changelog,
> tag, publish to crates.io, deploy book).
